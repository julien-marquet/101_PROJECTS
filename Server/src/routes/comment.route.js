module.exports = (api, controller, access, sender) => {
    // api.post('comment/:projectId', access.check(['Student', 'Admin']), controller.post, sender);
    // api.put('comment/:commentId', access.check(['Student', 'Admin']), controller.put, sender);
};
